package com.mindtree.ttg.assessment.question3;

import java.util.List;

/**
 * Question 3 (Lowest Word-Frequency First) Counts the word frequency.
 * 
 * @author TTG
 * 
 */
public class LowestWordFrequencyFirst {

	/**
	 * Counts the word frequency in a given String.
	 * 
	 * @param sentence
	 *            a String which contains words and punctuation symbol like
	 *            period (.) and comma (,).
	 * @return the words and their corresponding frequency in a certain order.
	 *         The word with lowest frequency should be ordered first followed
	 *         up by other words with higher frequency. If there is a tie
	 *         between two or more words because their frequencies are equal,
	 *         the words are ordered as per their alphabetical order. Only words
	 *         with frequency greater than one should be considered for output.
	 */
	public List<String> getWordFrequency(String sentence) {

		return null;
	}
}
