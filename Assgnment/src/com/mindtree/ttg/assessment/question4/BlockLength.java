package com.mindtree.ttg.assessment.question4;

import java.util.List;

/**
 * Given a character matrix, you have to capture the length of blocks in each
 * row and column. Arrange all the block lengths in ascending order.
 * 
 * @author TTG
 * 
 */
public class BlockLength {

	/**
	 * 
	 * @param matrix
	 *            A character matrix of order M by N. M corresponds to number of
	 *            rows where 1 < M < 6 & N corresponds to number of columns
	 *            where 1 < N < 9. The entries or elements of the matrix are
	 *            case-insensitive i.e. ‘a’ and ‘A’ should be considered as the
	 *            same.
	 * @return All the block lengths corresponding to each row and column in the
	 *         matrix arranged in ascending order.
	 */
	public List<Integer> arrangeBlockLengths(Character[][] matrix) {

		return null;
	}
}
